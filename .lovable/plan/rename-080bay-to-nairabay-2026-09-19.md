# Rename 080Bay to Nairabay

## What will change
- Replace every user-facing `080Bay` brand mention with exact casing `Nairabay` across pages, forms, safety messages, admin screens, install prompts, and app documentation.
- Update every route's search and social metadata, the app manifest, and page descriptions to use `Nairabay`.
- Preserve existing `nairabay.com` web addresses, email addresses, code folders/imports, browser storage and cache keys, database behavior, Bay# identities, and archived plans.

## Verification
- Search the active project again for any remaining `080Bay` references.
- Confirm every content page still has complete page-specific search and social metadata.
- Check the generated site for errors and verify the homepage and install details show `Nairabay`.
