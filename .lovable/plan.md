# Rename nairaBay to 080Bay

## What will change
- Replace every user-facing `nairaBay` brand mention with `080Bay` across all pages, notices, forms, admin screens, and offline messages.
- Update page titles, descriptions, social-sharing metadata, install-app manifest text, and internal project documentation to use `080Bay`.
- Keep existing web addresses, code import paths, browser storage keys, database behavior, Bay# identities, and seller data unchanged so the live app continues working.

## Verification
- Search the project again for any remaining old brand text.
- Check the generated site for errors and confirm the homepage displays `080Bay`.
